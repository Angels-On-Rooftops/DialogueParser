flags = [
    # add dialogue flags here so the parser can check for them
    # these don't need to be anywhere special in the unity project
    # just make sure you upload these so other ppl have em
    "AcceptedMission",
    "TestFlag",
]